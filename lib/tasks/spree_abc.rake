namespace :spree_abc do
 # desc "Bootstrap is: migrating, loading defaults, sample data and seeding (for all extensions) invoking spree:create_admin and spree:load_products tasks"
 # task :bootstrap  do
    # spree's db:bootstrap, load defaults first, include sample data, admin user
 #   Rake::Task["db:bootstrap"].invoke
    # create website, assign sample data to the website.
 #   Rake::Task["spree:extensions:multi_site:bootstrap_multi_site"].invoke

 # end
end