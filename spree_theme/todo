#flaws
after theme release, if we modify page_layout tree, this would cause theme generating error. 


#todos
2.3 _update_param_value.js.erb, update <a> hover,selected param_value, client should be updated by ajax. 
3.  ajax update section, no need refresh when add section into template.

4.  add section cart and try add to cart.
 
5. 编辑器z-index 小于highlight cover, 导致无法选择section

6. container 缺省 width:100%, 手机模板需要           
7. product  缺省 width:100%, text-align:center;
8. product images in slide
9. minicart for mobile
10. 滑动窗口
11. rename login_form to sign_in_form
12. section font awesome1 support link

#bugs
get_css 12,13,'block', it is not right in generated css. 
set font-size:0, unset it does not work.
set input font-color:red, unset it does not work.
category 'brand' do not work.

user logged in, click copy template until timeout, user is redirected to login form, after login again, redirect to get:template_theme/copy, this would cause error, there is only post:template_theme/copy

# new features
disable taxon/post link

click effect