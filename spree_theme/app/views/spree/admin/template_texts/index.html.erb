<%= render :partial => 'sub_menu' %>
<% content_for :page_actions do %>  
  <li><%= button_link_to t("static_content.new_page"), new_object_url, :icon => 'icon-plus' %></li>
<% end %>

<% content_for :page_title do %>
   <%= t("static_content.static_pages") %>
<% end %>

<table class="index">
  <colgroup>
    <col style="width: 80%" />
    <col style="width: 10%" />
  </colgroup>
  <thead>
    <tr>
      <th><%= Spree::TemplateText.human_attribute_name(:title) %></th>
      <th class="actions"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @template_texts.each do |page| %>
      <tr class="<%= cycle('odd', 'even') %>" id="<%= dom_id page %>">
        <td>
          <%= page.name %>
        </td>
        <td  class="actions">
          <%= link_to_edit page, :no_text => true %>
          <%= link_to_delete page, :no_text => true %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>