
<% if @assigned_resources.present? %>    
      $('[data-lid=<%=@page_layout.id%>] > .config_resource' ).html( '' );
<% else %>    
      $('[data-lid=<%=@page_layout.id%>] > .config_resource' ).html( <%= raw @assigned_resources.map(&:name).to_json %> );           
<% end %>
