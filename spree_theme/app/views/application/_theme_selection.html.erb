<%
#params:  layouts, page_layout roots,  
#         selected, the selected theme.
%>
<select onchange="window.location.href=this.value" name="theme[id]" id="theme_id">
    <% options = template_themes.collect{|theme| [theme.title, preview_template_theme_path(theme)] } %>
    <%=  options_for_select(options, preview_template_theme_path(selected)) %>
</select>